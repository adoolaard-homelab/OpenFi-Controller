# Required: password to sign in to this controller's own UI (not a router password).
# The controller will not serve any page or API until this is set.
OPENFI_PASSWORD=

# Optional application configuration. Router credentials are added in the OpenFi UI.
# OPENFI_PORT=3000
# OPENFI_DATA_PATH=/data/routers.json (default in Docker)
