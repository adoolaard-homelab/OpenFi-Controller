"use client";
import { Search, Wifi, Cable } from "lucide-react";
import { useMemo, useState } from "react";
const devices = [{ hostname: "Abels-MBP", ipaddr: "192.168.10.204", macaddr: "C8:89:F3:EB:C6:B9", expires: 42494, type: "Wi-Fi" }, { hostname: "iPhone", ipaddr: "192.168.1.246", macaddr: "A6:D2:DB:88:AE:21", expires: 39513, type: "Wi-Fi" }];
const remaining = (seconds: number) => `${Math.floor(seconds / 3600)}h ${Math.floor(seconds % 3600 / 60)}m`;
export default function ClientsPage() { const [query, setQuery] = useState(""); const filtered = useMemo(() => devices.filter((d) => `${d.hostname} ${d.ipaddr} ${d.macaddr}`.toLowerCase().includes(query.toLowerCase())), [query]); return <div className="mx-auto max-w-7xl p-5 md:p-9"><p className="mb-2 text-xs font-semibold uppercase tracking-[.2em] text-sky-400">Clients</p><h1 className="text-3xl font-semibold tracking-tight">Client Devices</h1><p className="mt-2 text-sm text-slate-500">DHCP leases reported by your OpenWrt gateway.</p><section className="panel mt-8 overflow-hidden"><div className="flex flex-col gap-4 border-b border-slate-800 p-5 sm:flex-row sm:items-center sm:justify-between"><p className="text-sm text-slate-400"><b className="text-white">{filtered.length}</b> connected clients</p><label className="flex items-center gap-2 rounded-lg border border-slate-700 bg-slate-900 px-3 py-2 text-sm"><Search size={16} className="text-slate-500"/><input aria-label="Search clients" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search hostname or IP" className="w-52 bg-transparent outline-none placeholder:text-slate-600"/></label></div><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-slate-900/60 text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-4">Device</th><th className="px-5 py-4">IP address</th><th className="px-5 py-4">MAC address</th><th className="px-5 py-4">Lease expires</th><th className="px-5 py-4">Connection</th></tr></thead><tbody className="divide-y divide-slate-800">{filtered.map((device) => <tr key={device.macaddr} className="transition hover:bg-slate-900/50"><td className="px-5 py-4 font-medium">{device.hostname}</td><td className="px-5 py-4 text-slate-300">{device.ipaddr}</td><td className="px-5 py-4 font-mono text-xs text-slate-400">{device.macaddr}</td><td className="px-5 py-4 text-slate-300">{remaining(device.expires)}</td><td className="px-5 py-4"><span className="flex items-center gap-2 text-slate-300">{device.type === "Wi-Fi" ? <Wifi size={16} className="text-sky-400"/> : <Cable size={16} className="text-sky-400"/>}{device.type}</span></td></tr>)}{filtered.length === 0 && <tr><td colSpan={5} className="p-10 text-center text-slate-500">No clients match this search.</td></tr>}</tbody></table></div></section></div> }
